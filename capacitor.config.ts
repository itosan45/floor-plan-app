import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.floorplan',
  appName: 'floor-plan-editor',
  webDir: 'dist'
};

export default config;
